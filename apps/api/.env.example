# Copy to `.env`. Do not commit `.env`.
# Local Compose (repo root): PostgreSQL on 127.0.0.1:5432.
# User/password/database match docker-compose.yml — not production.

DATABASE_URL=postgresql://postgres:postgres@127.0.0.1:5432/postgres
JWT_SECRET=
