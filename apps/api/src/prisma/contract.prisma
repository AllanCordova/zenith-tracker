// use prisma-next

enum Role {
  @@type("pg/text@1")
  STUDENT = "STUDENT"
  TRAINER = "TRAINER"
}

model User {
  id           String @id @default(uuid())
  name         String
  email        String @unique
  passwordHash String
  role         Role
}
